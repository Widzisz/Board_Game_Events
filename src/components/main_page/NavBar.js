const NavBar = () => {
    return (
        <nav className="nav">
            <button className="nav__btn">
                <a href="#">Creat New Event</a>
            </button>
        </nav>
    );
};

export default NavBar;
